spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.1.22/smilemall_wms
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.22:8848
  application:
    name: cn-smile-smilemall-ware
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: cn.smile.smilemall.entity
  global-config:
    db-config:
      id-type: auto

server:
  port: 7001