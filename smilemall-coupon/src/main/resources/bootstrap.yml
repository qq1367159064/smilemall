spring:
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: 192.168.1.15:8848
        group: SMILE_COUPON
management:
  endpoints:
    web:
      exposure:
        include: "*"
